1️⃣ What is the difference between var, let, and const?
ans:- the difference between var, let, and const is

var:- var is a scoped function .It is hoisted and initialized any time . it declared value then again re-assign the value on the time. it ignore blocks{}.ver is a global function.it creates global properties.sometimes it can create bugs .thats why all developers avoid this function.

let:-Let is a block-scoped function.It is a hoisted function but not initialized .it can change  the value any time . It only works the block .Let can create re-assigned value.only this function can create value in many time.it is a safe function.use only if the value change.

const:- const also a block-scoped function.it can change the value one time.It is a re-declared function. Sometimes it returns accidental re-assignments

2️⃣ What is the spread operator (...)?

ans:- The spread operator provides any types of individual item like:-array,object,number etc.Use spread every value pass separately.

3️⃣ What is the difference between map(), filter(), and forEach()?

ans:-The difference between map(), filter(), and forEach() is

map():- map() transform each elements into an new array. It returns a new array with transformed value.But it not change original value . it change logical value .

filter():- filler() also dose not change the original value. It change the value with a condition.It returns only a filtered value .it use when filtered the certain value from the elements.

forEach():- ForEach() loop through each elements of array.it execute a function.It dose not return a new array.When want to perform an action for each elements.

4️⃣ What is an arrow function?
ans:- An arrow function can concise way to write function in js, introduce ES6.It makes traditional function to short function.It use parentheses to return parameters.

5️⃣ What are template literals?
ans:-template literal in js are a modern way to work with string,expression or multi-line text etc.Template literal are inclosed by backtick.It can includes expression inside string